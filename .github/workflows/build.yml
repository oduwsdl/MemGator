name: Build Binaries

on:
  - push
  - pull_request

jobs:
  build:
    runs-on: ubuntu-18.04
    steps:
      - uses: actions/checkout@master
      - uses: actions/setup-go@master
        with:
          go-version: '1.14'
      - name: Run Cross-build
        run: ./crossbuild.sh
